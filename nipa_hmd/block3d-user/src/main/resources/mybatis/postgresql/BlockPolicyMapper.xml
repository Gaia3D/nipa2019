<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hmd.persistence.postgresql.BlockPolicyMapper">

    <!-- 운영 정책 정보 조회 -->
    <select id="getBlockPolicy" resultType="blockPolicy">
        /* getBlockPolicy */
        SELECT * FROM block_policy
    </select>
    
</mapper>